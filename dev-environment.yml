name: seaflux-dev
channels:
  - defaults
dependencies:
  - python==3.7
  - pip
  - pytest-cov
  - flake8
  - wheel
  - xarray
  - black==19.10b0  # this is NB, otherwise will fail
  - isort
  - pandas
  - numpy
  - pip:
    - pre-commit
    - mkdocs
    - pooch


