name: pyseaflux
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (don't remove)
  - pytest
  # Examples (remove and add as needed)
  - numpy
  - scipy
  - pandas
  - xarray
  - netcdf4
  - pip
  - pip:
    - pooch
    - fetch-data
